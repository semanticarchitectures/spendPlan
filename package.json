{
  "name": "spendplan",
  "version": "0.2.0",
  "private": true,
  "description": "Personal financial planning, budgeting, statement import and what-if scenarios (static, no build step).",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "e2e": "node tests/e2e.js"
  },
  "devDependencies": {
    "playwright": "^1.47.0"
  }
}
